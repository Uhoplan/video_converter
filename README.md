Для запуска приложения, клонируйте репозиторий,  установите зависимости, в корне создайте .env файл, запустите приложение коммандой "node --env-file=.env app.js"

пример содержимого .env:

WEB_HOST=localhost

WEB_PORT=3000

WEB_SOCKET_PORT=8080
