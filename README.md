Для запуска приложения, 

- клонируйте репозиторий,
- установите зависимости (``cd video_converter && npm install``)
- в корне создайте .env файл(`` touch .env && echo WEB_HOST=localhost >> .env && echo WEB_PORT=3000 >> .env``),
- запустите приложение коммандой: (``node --env-file=.env app.js``)
